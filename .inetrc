%% -- ERLANG INET CONFIGURATION FILE --

%% Load resolve file
{resolv_conf,"/etc/resolv.conf"}.

%% Load hosts file
{file,hosts,"/etc/hosts"}.

%% Disable caching
{cache_size,0}.

%% Disable EDNS
{edns,false}.

%% Set lookup to native only
{lookup,[native]}.